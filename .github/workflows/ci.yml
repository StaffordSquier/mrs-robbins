name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: TypeScript compilation check
      run: npx tsc --noEmit
      
    - name: ESLint code quality check
      run: npm run lint
      continue-on-error: false
      
    - name: Build application
      run: npm run build
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

  spec-compliance:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Check Projects System - API Routes
      run: |
        echo "=== Projects System API Route Compliance ==="
        
        # Required API routes per technical spec
        test -f "app/api/projects/route.ts" || { echo "❌ Missing: app/api/projects/route.ts"; exit 1; }
        test -f "app/api/projects/[id]/route.ts" || { echo "❌ Missing: app/api/projects/[id]/route.ts"; exit 1; }
        test -f "app/api/projects/[id]/stats/route.ts" || { echo "❌ Missing: app/api/projects/[id]/stats/route.ts"; exit 1; }
        test -f "app/api/projects/[id]/duplicate/route.ts" || { echo "❌ Missing: app/api/projects/[id]/duplicate/route.ts"; exit 1; }
        
        echo "✅ All Projects API routes exist"
        
    - name: Check Projects System - TypeScript Types
      run: |
        echo "=== Projects System Type Definitions ==="
        
        # Required type definitions per spec
        test -f "lib/types/projects.ts" || { echo "❌ Missing: lib/types/projects.ts"; exit 1; }
        
        # Verify required interfaces exist in the file
        grep -q "interface Project" lib/types/projects.ts || { echo "❌ Mis